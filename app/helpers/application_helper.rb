# This file defines the ApplicationHelper, which is the parent helper for ALL helpers in your app.
# Helpers in Rails are modules that contain methods you can use inside your view templates
# (the HTML files that render pages). They keep complex logic out of your views.

# The "module" keyword creates a container for helper methods. Right now it's empty —
# that's normal! As your app grows, you can add shared helper methods here that any view can use.
module ApplicationHelper
end
