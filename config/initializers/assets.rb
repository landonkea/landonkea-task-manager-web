# This file configures the Rails asset pipeline, which handles your CSS, JavaScript,
# and image files. The asset pipeline compiles, bundles, and serves these files efficiently.

# Be sure to restart your server when you modify this file.

# Version of your assets, change this if you want to expire all your assets.

# `Rails.application.config.assets.version = "1.0"` sets a version number for your assets.
# When you change this number, all browsers will be forced to re-download your assets
# (because the URL fingerprints change). This is how you "expire" cached assets --
# useful when you deploy updates and need users to get the new versions immediately.
Rails.application.config.assets.version = "1.0"

# Add additional assets to the asset load path.
# Rails.application.config.assets.paths << Emoji.images_path
# The commented-out line above would add an extra folder to the list of places
# Rails looks for asset files. By default, Rails checks `app/assets/`, `lib/assets/`,
# and `vendor/assets/`. You'd add more paths if assets lived in unusual locations.
